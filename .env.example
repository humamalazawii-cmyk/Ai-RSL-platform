# ================== Database ==================
# Local dev (Docker):
DATABASE_URL="postgresql://rsl:rsl_dev_password@localhost:5432/rsl_ai"

# Production (Google Cloud SQL) — replaced via Secret Manager in prod

# ================== Auth ==================
# Investor password (default: RSL2026)
INVESTOR_PASSWORD="RSL2026"

# JWT secret - MUST be long random string in production (32+ chars)
JWT_SECRET="change-me-to-long-random-string-in-production"

# ================== App ==================
NODE_ENV="development"
